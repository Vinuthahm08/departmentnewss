<!DOCTYPE html>
<html>
<head>
  <title>Exam Results</title>
</head>
<body>
  <h1>Exam Results</h1>
  <% if (results.length > 0) { %>
    <ul>
      <% results.forEach(result => { %>
        <li><%= result.name %>: CGPA <%= result.cgpa %></li>
      <% }) %>
    </ul>
  <% } else { %>
    <p>No results found for the provided USN.</p>
  <% } %>
</body>
</html>
